#include <stdio.h>
#include <math.h>

int main ()
{
    int Raio, Result;

    printf ("Digite o raio da esfera.\n");
    scanf ("%d", &Raio);

    Result = (4 * 3,15 / 3) * Raio * Raio * Raio;

    printf ("\nO volume da esfera é %d um³", Result);

}